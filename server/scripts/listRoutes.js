// This diagnostic script was removed/replaced as part of consolidation of the app entrypoint.
// Keeping a minimal stub so that CI or developer tooling that references this file will not fail.
console.log(
  'listRoutes script intentionally disabled — route listing is now handled via `app` exports or separate diagnostics.'
)
process.exit(0)
